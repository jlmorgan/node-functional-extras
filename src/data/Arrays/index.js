"use strict";

// Project
const concat = require("./concat");
const equals = require("./equals");
const requireArray = require("./requireArray");

module.exports = {
  concat,
  equals,
  requireArray
};
