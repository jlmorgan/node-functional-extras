# `isUndefined(value)`

Determines whether or not the `value` is `undefined`.

## Arguments

* `value (*)`: The value.

## Returns

* `(Boolean)`: `true` if `undefined`; otherwise, `false`.

## Examples

```javascript
isUndefined();
// => true

isUndefined(null);
// => false
```
