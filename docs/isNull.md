# `isNull(value)`

Determines whether or not the `value` is `null`.

## Arguments

* `value (*)`: The value.

## Returns

* `(Boolean)`: `true` if `null`; otherwise, `false`.

## Examples

```javascript
isNull();
// => false

isNull(null);
// => true
```
