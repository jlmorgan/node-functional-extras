# `Tuple#swap()`

Creates a new [`Tuple`][Tuple] with swapped values.

## Returns

* `(Tuple)`: The swapped `Tuple`.

```javascript
const tuple = Tuple.of("a", 1);

tuple.swap();
// => Tuple(1, "a")
```

[Tuple]: ..
