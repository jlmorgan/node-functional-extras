# `Maybe.Nothing()`

Creates a nothing to represent `null` or a missing value.

## Types

* `A`: The underlying type.

## Returns

* `(Maybe)`: A `Nothing`.

## Example

```javascript
Maybe.Nothing();
// => Nothing()
```
