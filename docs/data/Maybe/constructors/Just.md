# `Maybe.Just(value)`

Creates a `Just` of the value.

## Arguments

* `value (*)`: A non-null value.

## Returns

* `(Maybe)`: A `Maybe` of the `value`.

## Example

```javascript
Maybe.Just("a");
// => Just(a)
```
