# `Either.Left(value)`

Encapsulates a left value.

## Arguments

* `value (*)`: A value.

## Returns

* `(Either)`: An `Either` of the `value`.

## Example

```javascript
Either.Left(new Error());
// => Left(Error)
```
