# `Either.Right(value)`

Encapsulates a right value.

## Arguments

* `value (*)`: The value.

## Returns

* `(Either)`: An `Either` of the `value`.

## Example

```javascript
Either.Right("a");
// => Right("a")
```
