# `id(a)`

The `I` combinator or identity morphism.

## Arguments

* `a (*)`: The input value.

## Returns

* `(*)`: The input value.

## Example

```javascript
const now = new Date();

id(now) === now;
// => true
```
