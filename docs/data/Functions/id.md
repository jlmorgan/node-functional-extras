# `id(a)`

The `I` combinator or identity morphism.

## Arguments

* `a (*)`: The value.

## Returns

* `(*)`: The value.

## Example

```javascript
const now = new Date();

id(now) === now;
// => true
```
