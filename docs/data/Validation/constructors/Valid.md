# `Validation.Valid(value)`

Encapsulates a valid value.

## Arguments

* `value (*)`: The value.

## Returns

* `(Validation)`: An `Validation` of the `value`.

## Example

```javascript
Validation.Valid("a");
// => Valid("a")
```
